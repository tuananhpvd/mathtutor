# Python
__pycache__/
*.py[cod]
.venv/
*.db
# Bản sao lưu CSDL trước khi chạy migration (vd dev.db.bak-20260720-182156). Không khớp
# "*.db" nên phải khai riêng — các file này CHỨA DỮ LIỆU HỌC SINH (họ tên, hash mật khẩu),
# lỡ commit là rò rỉ dữ liệu.
*.db.bak*
.env
*.egg-info/

# Node
node_modules/
dist/
.vite/

# Playwright E2E (kết quả chạy, trace, report — sinh lại mỗi lần chạy)
test-results/
playwright-report/

# Editor
.vscode/
.idea/
*.swp

# Dev server logs (detached supervisor)
dev-backend.log
dev-backend.err
dev-frontend.log
dev-frontend.err
logs/
*_backend.txt
*_frontend.txt
backend_out.txt
backend_err.txt
frontend_out.txt
frontend_err.txt

# Ảnh minh họa câu hỏi upload lúc chạy (không commit)
uploads/
backup_data.json
luong_chay
upload_dulieu

# PID runtime của dev-servers.ps1/dev-check.ps1 — chỉ có ý nghĩa trên máy dev đang chạy
scripts/.pid/
