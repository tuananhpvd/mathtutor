# Tùy chọn — bắt lỗi JS runtime phía HS/GV bằng Sentry (xem docs/PROGRESS.md mục 7).
# Để trống = không bật gì (an toàn mặc định, không phát request nào ra ngoài).
# Lấy DSN: đăng ký free tại sentry.io -> New Project (React) -> copy DSN.
VITE_SENTRY_DSN=
